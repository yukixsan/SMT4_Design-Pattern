#ifndef GHOST_H
#define GHOST_H

class Ghost {
public:
    int x, y;  // Ghost position
    char icon;  // Character representing the ghost (e.g., 'G')

    Ghost(); // Constructor (can be empty for now)
};

#endif
